import 'package:flutter/material.dart';

class T_and_CScreen extends StatelessWidget {
  const T_and_CScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('T and C Screen'),
      ),
    );
  }
}
